context("tidy")

## TODO: Rename context
## TODO: Add more tests

test_that("multiplication works", {
  example(dim_names)
  example(var_names)
  example(dimension_values)
  example(filtrate, ask = FALSE)
})
